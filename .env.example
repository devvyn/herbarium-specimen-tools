# Environment Configuration Template
# Copy to .env and fill in your values
# DO NOT commit .env to git!

# ============================================================================
# Environment
# ============================================================================
# Options: development, production
ENVIRONMENT=development

# ============================================================================
# JWT Authentication
# ============================================================================
# Generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# REQUIRED for production
JWT_SECRET_KEY=your-secret-key-here-change-this

# Token expiration in minutes (default: 1440 = 24 hours)
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ============================================================================
# User Authentication
# ============================================================================
# Format: username:hashed_password,username2:hashed_password2
# Generate hash: python -c "from passlib.context import CryptContext; pwd_context = CryptContext(schemes=['bcrypt'], deprecated='auto'); print(pwd_context.hash('your-password'))"
# REQUIRED for production
AUTH_USERS=curator:$2b$12$example_hash,reviewer:$2b$12$example_hash

# ============================================================================
# CORS Configuration
# ============================================================================
# Comma-separated list of allowed origins
# Development default: http://localhost:8000,http://127.0.0.1:8000
# Production: MUST be HTTPS only
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000

# ============================================================================
# Trusted Hosts (Production Only)
# ============================================================================
# Comma-separated list of allowed hostnames
# Only required if ENVIRONMENT=production
ALLOWED_HOSTS=yourdomain.com,mobile.yourdomain.com

# ============================================================================
# GBIF Configuration
# ============================================================================
# Minimum confidence score for taxonomy matches (0.0-1.0)
GBIF_MIN_CONFIDENCE=0.80

# Enable fuzzy name matching (true/false)
GBIF_FUZZY_MATCHING=true

# Enable occurrence validation (true/false)
# Note: Adds extra API calls to GBIF
GBIF_OCCURRENCE_VALIDATION=false

# ============================================================================
# Data Directories
# ============================================================================
# Path to directory containing raw.jsonl
EXTRACTION_DIR=./examples/sample_data

# Path to directory containing specimen images
IMAGE_DIR=./examples/sample_data/images

# ============================================================================
# Server Configuration
# ============================================================================
# Host to bind to (0.0.0.0 = all interfaces)
HOST=0.0.0.0

# Port to listen on
PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=info

# ============================================================================
# Logging (Optional)
# ============================================================================
# Use JSON format for structured logging (true/false)
# Recommended: false for development, true for production
LOG_JSON_FORMAT=false

# Log file path (optional, logs to stdout if not set)
# LOG_FILE=/var/log/herbarium-api.log

# ============================================================================
# Development Only
# ============================================================================
# Auto-reload on code changes (true/false)
# Only works in development mode
# RELOAD=true
